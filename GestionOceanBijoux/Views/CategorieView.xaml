<Page x:Class="GestionOceanBijoux.Views.CategorieView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GestionOceanBijoux.Views"
      xmlns:viewModel="clr-namespace:GestionOceanBijoux.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewModel:CategorieViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CategorieView">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#D9C2A9"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#F5EFE6"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#412E20"/>
        <SolidColorBrush x:Key="TextBrush" Color="#412E20"/>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="RowBackground" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="AlternatingRowBackground" Value="White" />
            <Setter Property="GridLinesVisibility" Value="Vertical" />
            <Setter Property="VerticalGridLinesBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource PrimaryBrush}" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Height" Value="35"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- FORMULAIRE D'AJOUT -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBrush}" Padding="20" Margin="20" CornerRadius="10">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Width="400">
                <TextBlock Text="Ajouter une nouvelle catégorie" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,10" TextAlignment="Center"/>

                <TextBlock Text="Nom de la catégorie"/>
                <TextBox Text="{Binding NomCategorie, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Background="White"/>

                <Button Content="Ajouter la catégorie" Command="{Binding AjouterCategorieCommand}" Background="{StaticResource PrimaryBrush}" Foreground="White" FontWeight="Bold" Padding="8" Margin="0,10,0,0" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- DATAGRID DES CATÉGORIES -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Categories}" Width="Auto" Height="Auto" Margin="20" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding id}" Width="80"/>
                <DataGridTextColumn Header="Nom de la catégorie" Binding="{Binding categorie}" Width="300"/>
                <DataGridTemplateColumn Header="Supprimer" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🗑" Command="{Binding DataContext.SupprimerCategorieCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Padding="5" ToolTip="Supprimer cette catégorie"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
