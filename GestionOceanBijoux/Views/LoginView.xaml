<Window x:Class="GestionOceanBijoux.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Connexion" 
        Height="420" Width="500" 
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA"
        ResizeMode="NoResize">

    <Grid>
        <!-- Cadre blanc au centre -->
        <Border Background="White" CornerRadius="10" Padding="20" 
                VerticalAlignment="Center" HorizontalAlignment="Center"
                Width="450" Height="350" BorderBrush="#DDD" BorderThickness="1"
                >

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Connexion" FontSize="24" FontWeight="Bold" Foreground="#333"
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Email" FontSize="14" Margin="0,0,0,5"/>
                <TextBox x:Name="EmailTextBox" Height="45" Width="380" Text="oceanebondon30@gmail.com"
                         Style="{StaticResource {x:Type TextBox}}" Cursor="" />
                <TextBlock Text="Mot de passe" FontSize="14"/>
                <PasswordBox x:Name="PasswordBox" Height="45" Width="380" Password="Ilona2005."
                             Style="{StaticResource {x:Type PasswordBox}}" />

                <Button Content="Se connecter" Click="LoginButton_Click"
                        Width="380" Height="45"
                        IsDefault="True"
                        Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
