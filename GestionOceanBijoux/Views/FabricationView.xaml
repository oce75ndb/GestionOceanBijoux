<Page x:Class="GestionOceanBijoux.Views.FabricationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GestionOceanBijoux.Views"
      xmlns:helpers="clr-namespace:DogsitterCRUD.Helpers"
      xmlns:viewModel="clr-namespace:GestionOceanBijoux.ViewModels"
      d:DataContext="{d:DesignInstance Type=viewModel:FabricationViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FabricationView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- FORMULAIRE D'AJOUT -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBrush}" Padding="20" Margin="20" CornerRadius="10">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Width="400">
                <TextBlock Text="Ajouter une nouvelle fabrication" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,10" TextAlignment="Center"/>

                <TextBlock Text="Matériau"/>
                <TextBox Text="{Binding Fabrication, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Background="White"/>

                <Button Content="Ajouter la fabrication" Command="{Binding AjouterFabricationCommand}" Background="{StaticResource PrimaryBrush}" Foreground="White" FontWeight="Bold" Padding="8" Margin="0,10,0,0" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <ProgressBar Grid.Row="1" Height="20" Width="200" IsIndeterminate="True" VerticalAlignment="Top"   
            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" Foreground="{StaticResource AccentBrush}" Background="{StaticResource PrimaryBrush}"/>

        <!-- DATAGRID DES FABRICATIONS -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Fabrications}" Width="Auto" Height="Auto" Margin="20" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding id}" Width="80"/>
                <DataGridTextColumn Header="Fabrication" Binding="{Binding fabrication}" Width="*"/>
                <DataGridTemplateColumn Header="Supprimer" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🗑" Command="{Binding DataContext.SupprimerFabricationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Padding="5" ToolTip="Supprimer cette fabrication"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
